DB_NAME=
DB_USER=
DB_PASSWORD=

# JWT secret key used to sign tokens. Use a long, random value (e.g., a 32+ character string or a Base64-encoded key).
JWT_SECRET=
# JWT expiration time in milliseconds (e.g., 3600000 for 1 hour, 86400000 for 24 hours).
JWT_EXPIRATION=